# Caso de Uso: UC-004

## Nome
Busca Automatica de Certidoes

## Objetivo
Permitir que o usuario solicite a busca automatica de certidoes da empresa (INSS, FGTS, tributos federais) em portais governamentais, com resolucao automatica de CAPTCHAs, obtendo os documentos e seus status de validade.

## Escopo
Modulo Empresa — EmpresaPage.tsx > secao "Certidoes Automaticas" (quarta secao da pagina) / Backend endpoint `POST /api/empresa-certidoes/buscar-stream`

> **Nota de implementacao:** A EmpresaPage nao possui abas — as certidoes estao na secao "Certidoes Automaticas", quarta secao da pagina.

## Atores
- Ator primario: Usuario autenticado (gestor documental da empresa)
- Ator secundario: Sistema automatico (browser headless com CAPSolver)

## Gatilho
O usuario acessa a aba "Certidoes" na EmpresaPage e clica no botao de buscar certidoes.

## Pre-condicoes
1. O usuario esta autenticado com JWT valido
2. Empresa cadastrada com CNPJ valido
3. Fontes de certidao configuradas ou sincronizadas
4. Endpoints de certidoes operacionais no backend
5. O sistema (frontend e backend) esta operacional

## Pos-condicoes
1. As certidoes obtidas estao persistidas na tabela "empresa_certidoes"
2. Cada certidao possui status (valida, vencida, pendente, erro, nao_disponivel)
3. Arquivos PDF das certidoes estao salvos no servidor (path_arquivo)
4. Datas de obtencao e validade estao registradas

## Regras de negocio
- RN-CERT-001: Certidoes tem validade e o sistema calcula o status automaticamente (valida, vencida, pendente)
- RN-CERT-002: A busca automatica usa browser headless (certidao_browser.py) com CAPSolver para resolver CAPTCHAs
- RN-CERT-003: Nem todas as certidoes permitem busca automatica (campo permite_busca_automatica)
- RN-CERT-004: O status "buscando" e exibido durante o processamento
- RN-CERT-005: Fontes de certidoes sao configuraveis via tabela fontes_certidoes
- RN-CERT-006: Cada certidao registra orgao emissor, numero, URL de consulta e dados extras

## Componentes de UI envolvidos
- EmpresaPage > secao "Certidoes Automaticas"
- DataTable com colunas: certidao, tipo, status, data_obtencao, validade, orgao_emissor, acoes
- StatusBadge com cores por status:
  - valida: verde
  - vencida: vermelho
  - pendente: amarelo
  - buscando: azul com animacao (Loader2) — exibido durante progresso streaming
  - erro: vermelho com icone AlertCircle
  - nao_disponivel: cinza
- Botao `Buscar Certidoes` — valida `empresaId` e `cnpj` e chama o fluxo streaming
- Botao `Editar certidao` — abre modal de ajuste de numero, validade, orgao e status
- Botao `Upload PDF` — anexa PDF manual a uma certidao existente
- Botao `Download PDF` — baixa o PDF vinculado
- Botao `Atualizar esta certidao` — reconsulta uma certidao especifica
- Botao `Abrir portal` — abre a URL de consulta da fonte emissora
- Modal de detalhe com botoes `Salvar`, `Abrir Portal`, `Download`, `Fechar`
- Modal de upload com botoes `Enviar` e `Cancelar`

## Endpoints ou servicos envolvidos
- POST /api/empresa-certidoes/buscar-stream — inicia busca streaming por CNPJ (endpoint principal de busca automatica)
- POST /api/empresa-certidoes/{id}/upload — anexa PDF manual a uma certidao existente
- GET /api/crud/empresa-certidoes — lista certidoes da empresa (recarregado apos busca)
- PUT /api/crud/empresa-certidoes/{id} — atualiza certidao (edicao manual de numero, validade, orgao, status)
- DELETE /api/crud/empresa-certidoes/{id} — remove certidao

## Dados manipulados
- Entrada: empresa_id, CNPJ da empresa (validados antes do disparo do streaming)
- Saida: objeto CertidaoAutomatica com campos: id, certidao, tipo, status, data_obtencao, validade, path_arquivo, url_consulta, orgao_emissor, fonte_certidao_id, fonte_nome, numero, permite_busca_automatica, mensagem, dados_extras
- Armazenamento: tabela `empresa_certidoes` no MySQL + arquivos PDF no filesystem

### Persistencia observada (UC-F04)
Campos confirmados na tabela `empresa_certidoes`:
`tipo`, `orgao_emissor`, `numero`, `data_vencimento`, `path_arquivo`, `status`, `url_consulta`, `fonte_certidao_id`, `mensagem`, `dados_extras`

## Fluxo principal

### Passo 1: Acesso a secao Certidoes
- **Acao:** O ator rola a pagina ate a secao "Certidoes Automaticas" dentro da EmpresaPage
- **Elemento UI:** Secao "Certidoes Automaticas" (quarta secao da pagina)
- **Resposta visual:** O sistema renderiza a lista de certidoes da empresa; exibe DataTable com certidoes ja obtidas (se existirem) e seus status; exibe botao `Buscar Certidoes`
- **Efeito funcional:** GET /api/crud/empresa-certidoes e invocado para carregar certidoes existentes
- **Requisicao:** GET /api/crud/empresa-certidoes com filtro por empresa_id
- **Persistencia:** Nenhuma (apenas leitura)
- **Evidencia:** Screenshot da secao Certidoes (runtime/screenshots/UC-004/01_aba_certidoes.png)

### Passo 2: Iniciar busca automatica
- **Acao:** O ator clica no botao `Buscar Certidoes`
- **Elemento UI:** Botao `Buscar Certidoes`
- **Resposta visual:** O sistema valida `empresaId` e `cnpj`; inicia progresso streaming por fonte consultada; os StatusBadge das certidoes em processamento mudam para "buscando" (azul com icone Loader2 girando)
- **Efeito funcional:** Sistema chama `POST /api/empresa-certidoes/buscar-stream`; interface mostra progresso streaming por fonte consultada
- **Requisicao:** POST /api/empresa-certidoes/buscar-stream com empresaId e cnpj
- **Persistencia:** Registros em `empresa_certidoes` sao atualizados progressivamente por fonte
- **Evidencia:** Screenshot do estado de busca em andamento (runtime/screenshots/UC-004/02_buscando.png)

### Passo 3: Processamento automatico (sistema)
- **Acao:** Nenhuma do ator — o sistema executa automaticamente
- **Elemento UI:** StatusBadge "buscando" permanece visivel
- **Resposta visual:** O sistema mantem indicadores de loading enquanto o browser headless acessa os portais governamentais
- **Efeito funcional:** certidao_browser.py acessa sites do INSS, FGTS, Receita Federal; CAPSolver resolve CAPTCHAs automaticamente; PDFs sao baixados e salvos
- **Requisicao:** Requisicoes HTTP do browser headless aos portais governamentais
- **Persistencia:** Arquivos PDF salvos no filesystem; registros atualizados progressivamente na tabela empresa_certidoes
- **Evidencia:** Nenhuma visual direta (processamento em background)

### Passo 4: Resultado da busca
- **Acao:** O ator observa os resultados apos conclusao da busca (pode precisar atualizar a pagina)
- **Elemento UI:** DataTable atualizada com novas certidoes
- **Resposta visual:** Os StatusBadge mudam de "buscando" para o status final: "valida" (verde), "vencida" (vermelho), "erro" (vermelho com icone) ou "nao_disponivel" (cinza); campos de data_obtencao e validade sao preenchidos
- **Efeito funcional:** GET /api/crud/empresa-certidoes e re-executado para carregar resultados atualizados
- **Requisicao:** GET /api/crud/empresa-certidoes
- **Persistencia:** Nenhuma (leitura dos dados ja persistidos no passo 3)
- **Evidencia:** Screenshot do resultado da busca (runtime/screenshots/UC-004/03_resultado_busca.png)

### Passo 5: Visualizar certidao obtida
- **Acao:** O ator clica no botao Eye de uma certidao com status "valida"
- **Elemento UI:** ActionButton com icone Eye
- **Resposta visual:** O sistema abre o PDF da certidao em nova aba ou modal de preview
- **Efeito funcional:** O arquivo PDF e servido a partir do path_arquivo salvo no servidor
- **Requisicao:** GET do arquivo PDF
- **Persistencia:** Nenhuma
- **Evidencia:** Screenshot do preview da certidao (runtime/screenshots/UC-004/04_preview_certidao.png)

### Passo 6: Verificacao de validade
- **Acao:** O ator observa as datas de validade e os status de cada certidao
- **Elemento UI:** Colunas validade e status no DataTable
- **Resposta visual:** Certidoes validas exibem badge verde; certidoes proximas do vencimento exibem alerta; certidoes vencidas exibem badge vermelho
- **Efeito funcional:** Nenhum (apenas visualizacao)
- **Requisicao:** Nenhuma
- **Persistencia:** Nenhuma
- **Evidencia:** Screenshot dos status de validade (runtime/screenshots/UC-004/05_status_validade.png)

## Fluxos alternativos

### FA-001: Edicao manual de certidao
1. O ator clica em `Editar certidao` em uma certidao especifica
2. O sistema abre modal de detalhe com campos editaveis: numero, validade, orgao e status
3. Modal exibe botoes `Salvar`, `Abrir Portal`, `Download`, `Fechar`
4. O ator edita os campos e clica em `Salvar`
5. PUT /api/crud/empresa-certidoes/{id} e invocado

### FA-002: Anexar PDF manual
1. O ator clica em `Upload PDF` em uma certidao sem arquivo
2. Sistema abre modal de upload com botoes `Enviar` e `Cancelar`
3. O ator seleciona o PDF e clica em `Enviar`
4. POST /api/empresa-certidoes/{id}/upload e invocado

### FA-003: Atualizar certidao individual
1. O ator clica em `Atualizar esta certidao` em uma certidao especifica
2. O sistema reconsulta apenas aquela certidao
3. Resultado e atualizado para aquela certidao

### FA-004: Abrir portal da certidao
1. O ator clica em `Abrir portal` em uma certidao
2. O sistema abre a URL de consulta da fonte emissora em nova aba

## Fluxos de excecao

### FE-001: Falha na resolucao de CAPTCHA
1. O CAPSolver nao consegue resolver o CAPTCHA de um portal
2. O status da certidao muda para "erro" com mensagem explicativa
3. O ator pode tentar novamente mais tarde

### FE-002: Portal governamental indisponivel
1. O site do orgao emissor esta fora do ar
2. O status da certidao muda para "erro" com mensagem "Portal indisponivel"
3. O ator pode tentar novamente

### FE-003: CNPJ invalido ou nao encontrado
1. O CNPJ da empresa nao e encontrado no portal consultado
2. O status da certidao muda para "nao_disponivel"
3. Mensagem explicativa e registrada no campo mensagem

### FE-004: CAPSolver nao configurado
1. A chave de API do CAPSolver nao esta no .env
2. O sistema retorna erro informando que a busca automatica nao esta disponivel
3. O ator deve cadastrar certidoes manualmente

## Evidencias esperadas
- Screenshot da aba Certidoes com listagem
- Screenshot do estado "buscando" durante processamento
- Screenshot do resultado com certidoes obtidas e seus status
- Screenshot de preview de certidao PDF
- Screenshot de certidao com status "erro"

## Criterios de validacao
1. A aba "Certidoes" renderiza com DataTable e botao de busca
2. Botao "Buscar Certidoes" aciona o processamento automatico
3. StatusBadge reflete corretamente cada estado (valida, vencida, pendente, buscando, erro, nao_disponivel)
4. Certidoes obtidas possuem PDF salvo e visualizavel
5. Datas de obtencao e validade sao preenchidas corretamente
6. Erros sao tratados e exibidos com mensagens claras

## Teste Playwright associado
- Arquivo: tests/e2e/playwright/uc-004.spec.ts
- Cenarios:
  - Abrir aba Certidoes e verificar renderizacao
  - Verificar exibicao de certidoes existentes com status correto
  - Acionar busca automatica e verificar estado "buscando"
  - Verificar resultado apos conclusao da busca
  - Visualizar certidao PDF

## Screenshots esperados
- runtime/screenshots/UC-004/01_aba_certidoes.png
- runtime/screenshots/UC-004/02_buscando.png
- runtime/screenshots/UC-004/03_resultado_busca.png
- runtime/screenshots/UC-004/04_preview_certidao.png
- runtime/screenshots/UC-004/05_status_validade.png

## Resultado da validacao
Pendente

## Divergencias encontradas
Nenhuma (pendente validacao)

## Correcoes realizadas
Nenhuma

## Status final
IMPLEMENTADO (confirmado por UC-F04 do documento fonte)

## Implementacao Observada

**Status:** IMPLEMENTADO

**Fonte:** UC-F04 (Buscar, revisar e anexar certidoes) do documento `CASOS DE USO EMPRESA PORTFOLIO PARAMETRIZACAO.md`.

**Observacoes criticas:**
- O endpoint real de busca automatica e `POST /api/empresa-certidoes/buscar-stream` (streaming), nao `tool_buscar_certidoes`.
- A busca valida `empresaId` e `cnpj` antes de disparar o streaming.
- A interface mostra progresso por fonte consultada em tempo real (streaming).
- Ao concluir, o sistema recarrega `empresa_certidoes` automaticamente.
- O upload manual de PDF usa endpoint especializado `/api/empresa-certidoes/{id}/upload`.
- O botao `Abrir portal` abre a `url_consulta` da fonte emissora.
- A edicao manual permite corrigir numero, validade, orgao e status individualmente.
- A EmpresaPage nao possui abas — as certidoes estao na quarta secao da pagina.
