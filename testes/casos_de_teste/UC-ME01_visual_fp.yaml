uc_id: UC-ME01
variacao: fp
trilha: visual
descricao: "Dashboard TAM/SAM/SOM"

passos:
  - id: passo_00_navegar_mercado
    asserts_dom:
      - selector: '.page-header h1, .page-header h2, h1, h2'

  - id: passo_01_validar_tam_sam_som
    # Sem assert (validacao via screenshot pos-execucao)
