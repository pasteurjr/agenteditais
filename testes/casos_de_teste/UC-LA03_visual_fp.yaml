uc_id: UC-LA03
variacao: fp
trilha: visual
descricao: "Sala Virtual de Disputa (LancesPage + Simulador)"

passos:
  - id: passo_00_navegar_lances
    asserts_dom:
      - selector: '.page-header h1, .page-header h2, h1, h2'

  - id: passo_01_validar_sala
    # Sem assert (validacao via screenshot)
